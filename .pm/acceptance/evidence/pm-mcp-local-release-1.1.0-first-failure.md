# 0.3.0 首次失败保留记录

本轮起点是已有的 0.2.0 本地候选。新增六项核心回归在实现之前全部失败：任务分页、游标失效、未知计数默认拒绝、兼容策略的证据标签、运行版本输出，以及已绑定项目的安全配置升级。

首次相关兼容检查保留了质量报告新增字段及仪表盘新展示导致的两项断言失败；这些断言被更新为验证新的明确契约，没有删除原始摘要或失败状态要求。

后续真实执行回归暴露了两个问题并保留失败结果：

- Node 的内部 NODE_TEST_CONTEXT 被传给子进程，会让嵌套 node --test 跳过文件却退出0。已隔离该内部标记，并增加断言验证子测试确实运行一项；没有放松未知计数门禁。
- 过宽的 Node 计数行匹配误识别 Vitest 摘要。已限定完整诊断行，分别核对各运行器来源和计数一致性。

原始输出保存在本轮独立备份目录 pm-upgrade-030-e56f247a373d448dbb7d0829b9ce09a0，文件包括 before.txt、focused-first.txt、focused-second.txt、focused-third.txt 和 boundaries-final.txt。后一次通过不覆盖这些失败。

本文件是失败范围与修复过程的复核说明，不是测试通过数据；当前通过与否由质量执行报告和冻结断言独立决定。

首次完整测试为 276 项、275 通过、1 失败。失败来自任务资源兼容用例所要求的“已截断”说明被改写；已保留该说明，同时加强用例验证 continuation 能通过真实 MCP 取回余下全部任务。该次质量矩阵在测试失败后停止，未执行后续构建、类型和覆盖率阶段；原始 full-first.test.txt 与相应失败质量报告均保留。
