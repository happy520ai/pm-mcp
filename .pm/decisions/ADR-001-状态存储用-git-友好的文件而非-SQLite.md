# ADR-001 状态存储用 git 友好的文件而非 SQLite

- 日期: 2026-09-01
- 状态: 已接受

## 背景

状态需要随代码版本化、被人和任意 AI 客户端直接读、跨工具共享（ZCode/Codex/Cursor）。

## 决定

分域 JSON + 追加式台账 + ADR 不可变编号，全部放 .pm/ 目录随 git 提交；MCP 只是带校验的读写 API。

## 后果

放弃 SQL 查询能力换取 diff 可读与合并友好；大规模聚合统计交给快照聚合。
