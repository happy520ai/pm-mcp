# pm-mcp 20 GiB / 约 3 亿行源码仓库基准

- 运行 ID: `fedd5d9f-e81d-46ca-9904-50b9e9150e06`
- 时间: 2026-09-02 20:05:59–20:46:08（Asia/Shanghai）
- 结果: **通过（complete）**
- 原始结果: `volume-20g-3e8-20260902.json`
- 原始结果 SHA-256: `D8AFD6005D72646BFD13FC84636D2F972842B8674670DA53BCA7758F7D561EC6`
- 本次执行脚本 SHA-256: `508A6E281FA6AE0744B3C445FED1F188B27BDF7E6C361CA4251572E5CD606CD6`

## 测试口径

- 20,480 个普通 `.ts` 文本文件，每个精确 1,048,576 字节。
- payload 精确 21,474,836,480 字节（20 GiB）。
- 每文件 14,567 行，总计 298,332,160 行（约 2.983 亿行）。
- 全部文件低于 2 MiB oversize 阈值；不使用稀疏文件、符号链接、压缩包或单个巨文件凑容量。
- 环境: Windows 10.0.19045 x64，Node v25.8.1，6 逻辑 CPU，15.94 GiB RAM，NTFS E:。
- 这是刚写入后的合成、单进程、低并发基准，不是冷盘、真实业务代码或生产 SLA 证明。

## 结果

| 阶段 | 耗时 | 内容吞吐 | 关键结果 |
|---|---:|---:|---|
| 生成 20 GiB | 66.461s | 308.15 MiB/s | 20,480 文件；卷可用空间减少约 19.589 GiB |
| 独立 stat + 样本 SHA-256 | 6.327s | — | 文件数、最小/最大大小、总字节完全匹配 |
| 首次结构内容索引 | 385.786s | 53.09 MiB/s | changed=20,480；hits=0；skippedDeep=0 |
| 暖结构走查 | 3.034s | — | changed=0；hits=20,480 |
| watcher 启动精确对账 | 2.251s | — | 强制使用 walk |
| watcher 稳态 `audit_structure` | 0.053s | — | SQL 快路径；不能冒充 20 GiB 内容扫描 |
| `snapshot_codebase` | 0.035s | — | 20,480 文件、298,332,160 LOC |
| 停机等量替换后重启对账 | 2.406s | — | fresh=false → walk → watcher；容量/LOC 不变 |
| 安全内容扫描 | 1,785.115s | 11.47 MiB/s | 独立读取 20 GiB；cache hits=0；open/high=0 |
| 许可证内容扫描 | 152.560s | 134.24 MiB/s | 再次独立读取 20 GiB；cache hits=0；无许可证红旗 |

总墙钟时间 2,409.103 秒（40 分 09.103 秒）；观测峰值 RSS 约 500.11 MiB。

## 正确性 oracle

- 文件系统 stat: 20,480 文件、21,474,836,480 字节。
- SQLite: `COUNT=20,480`、`SUM(size)=21,474,836,480`、`SUM(loc)=298,332,160`。
- SQLite: `content_ok=20,480`、`oversize=0`、`skip=0`。
- Snapshot: `total_files=20,480`、`total_loc=298,332,160`。
- Security: `disk_read_files=20,480`、`disk_read_bytes=21,474,836,480`、发现 0。
- License: `disk_read_files=20,480`、`disk_read_bytes=21,474,836,480`、缓存命中 0。
- 所有权清理: verified=true、removed=true；fixture 根已复查不存在。

## 证据边界

- 证明的是“20 GiB / 20,480 文件 / 约 2.983 亿行”这一数据形态；不等于 100 万小文件形态。
- `fs.watch` 稳态 53ms 证明 SQL 聚合快路径，不代表重新读取了 20 GiB。
- 安全扫描为离线启发式，不替代专业 SAST/CVE/供应链服务。
- 本次干净 payload 未植入尾部安全/GPL canary；规则检出能力由项目单元/红队测试覆盖，容量测试验证的是全字节读取覆盖。
- 当前项目根不是 Git 仓库，本次未覆盖大仓库 `git status` 性能与托管 CI。
